spring:
  profiles:
    include: db,oauth2,jwt

  #JPA
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        show_sql: true

logging:
  level:
    org.apache.http: DEBUG
    httpclient.wire: DEBUG